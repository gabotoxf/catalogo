# Mismo origen: el monolito sirve API + frontend desde un solo dominio,
# así el build funciona en cualquier URL (Render, Railway, local).
VITE_API_BASE_URL=/api
VITE_IMAGE_BASE_URL=/assets/img/Productos
VITE_CAT_IMAGE_BASE_URL=/assets/img/Categorias
